# t_aw9523 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Create a standalone debug test project that validates all 16 GPIO pins of the AW9523BTQR IO expander via I2C on the DeepStoa v1 board.

**Architecture:** Single-file ESP-IDF application (`t_aw9523.c`) that drives the existing `aw9523` driver component through six sequential test phases: identity check, output pin walk, input pin read, port-level ops, safe-state restore, and summary. Follows the exact project structure of the existing `debug/t_display/` reference.

**Tech Stack:** ESP-IDF v5.5.3, C (FreeRTOS), AW9523 I2C GPIO expander driver

## Global Constraints

- ESP-IDF v5.5.3, target esp32s3
- Match `debug/t_display/` project structure and sdkconfig.defaults
- Use existing `drivers/aw9523` component (no modifications)
- Use existing `boards/esp32s3/deepstoa_v1.h` pin definitions
- Config: main task stack 8192, WDT timeout 120s

---

## File Structure

| File | Responsibility |
|------|----------------|
| `debug/t_aw9523/CMakeLists.txt` | ESP-IDF project, points EXTRA_COMPONENT_DIRS at aw9523 driver |
| `debug/t_aw9523/main/CMakeLists.txt` | Main component, PRIV_REQUIRES aw9523 |
| `debug/t_aw9523/main/t_aw9523.c` | All 6 test phases in app_main() |
| `debug/t_aw9523/sdkconfig.defaults` | ESP-IDF config (copied from t_display) |

### Task 1: Create project scaffold (CMakeLists files + sdkconfig.defaults)

**Files:**
- Create: `debug/t_aw9523/CMakeLists.txt`
- Create: `debug/t_aw9523/main/CMakeLists.txt`
- Create: `debug/t_aw9523/sdkconfig.defaults`

**Interfaces:**
- Produces: Buildable ESP-IDF project that compiles `t_aw9523.c` with `aw9523` component linked

- [ ] **Step 1: Create project-level CMakeLists.txt**

```cmake
# debug/t_aw9523/CMakeLists.txt
cmake_minimum_required(VERSION 3.16)

# Point to drivers/ directory for extra component (aw9523)
set(EXTRA_COMPONENT_DIRS
    ${CMAKE_SOURCE_DIR}/../../drivers/aw9523
)

include($ENV{IDF_PATH}/tools/cmake/project.cmake)
project(t_aw9523)
```

- [ ] **Step 2: Create main component CMakeLists.txt**

```cmake
# debug/t_aw9523/main/CMakeLists.txt
idf_component_register(
    SRCS "t_aw9523.c"
    INCLUDE_DIRS "." "../../../boards/esp32s3"
    REQUIRES driver
    PRIV_REQUIRES aw9523
)
```

- [ ] **Step 3: Create sdkconfig.defaults**

```
# debug/t_aw9523/sdkconfig.defaults
# Set main task stack size to 8192
CONFIG_ESP_MAIN_TASK_STACK_SIZE=8192

# Increase task watchdog timeout
CONFIG_ESP_TASK_WDT_TIMEOUT_S=120
```

- [ ] **Step 4: Verify build system works**

Run: `idf.py build` from `debug/t_aw9523/`
Expected: CMake configures successfully (compilation will fail until t_aw9523.c exists, but project structure is valid)

- [ ] **Step 5: Commit**

```bash
git add debug/t_aw9523/CMakeLists.txt debug/t_aw9523/main/CMakeLists.txt debug/t_aw9523/sdkconfig.defaults
git commit -m "feat(debug): add t_aw9523 project scaffold"
```

### Task 2: Implement test firmware (t_aw9523.c)

**Files:**
- Create: `debug/t_aw9523/main/t_aw9523.c`

**Interfaces:**
- Consumes: `aw9523_init()`, `aw9523_get_chip_id()`, `aw9523_set_pin()`, `aw9523_get_pin()`, `aw9523_write_port()`, `aw9523_read_port()`, `aw9523_set_port_dir()` from `aw9523.h`
- Consumes: `DEEPV1_I2C_PORT`, `DEEPV1_PIN_I2C_SDA`, `DEEPV1_PIN_I2C_SCL`, `DEEPV1_PIN_IO_RESET`, `DEEPV1_AW9523_ADDR`, `AW9523_PIN()`, `AW9523_GET_PORT()`, `AW9523_GET_BIT()` from `deepstoa_v1.h`

- [ ] **Step 1: Write Phase 1 — I2C init and chip ID check**

```c
// debug/t_aw9523/main/t_aw9523.c
// AW9523 IO Expander: full pin-level validation test
#include <stdio.h>
#include <string.h>
#include "freertos/FreeRTOS.h"
#include "freertos/task.h"
#include "esp_log.h"
#include "esp_err.h"

#include "deepstoa_v1.h"
#include "aw9523.h"

static const char *TAG = "t_aw9523";

// Output pin list: all pins EXCEPT P0.5 (INT) and P1.6 (BUSY) which are inputs
static const uint8_t g_output_pins[] = {
    AW9523_PIN(0, 0), AW9523_PIN(0, 1), AW9523_PIN(0, 2), AW9523_PIN(0, 3),
    AW9523_PIN(0, 4), AW9523_PIN(0, 6), AW9523_PIN(0, 7),
    AW9523_PIN(1, 0), AW9523_PIN(1, 1), AW9523_PIN(1, 2), AW9523_PIN(1, 3),
    AW9523_PIN(1, 4), AW9523_PIN(1, 5), AW9523_PIN(1, 7),
};
static const int g_num_output_pins = sizeof(g_output_pins) / sizeof(g_output_pins[0]);

static const uint8_t g_input_pins[] = {
    AW9523_PIN(0, 5),  // LCD_INT
    AW9523_PIN(1, 6),  // LCD_BUSY
};
static const int g_num_input_pins = sizeof(g_input_pins) / sizeof(g_input_pins[0]);

void app_main(void)
{
    ESP_LOGI(TAG, "=== t_aw9523: AW9523 IO Expander Validation ===");

    // === Phase 1: I2C & Identity ===
    ESP_LOGI(TAG, "--- Phase 1: Init & Chip ID ---");
    esp_err_t ret = aw9523_init(DEEPV1_I2C_PORT,
                                DEEPV1_PIN_I2C_SDA,
                                DEEPV1_PIN_I2C_SCL,
                                DEEPV1_PIN_IO_RESET,
                                DEEPV1_AW9523_ADDR);
    if (ret != ESP_OK) {
        ESP_LOGE(TAG, "FAIL: aw9523_init returned %d", ret);
        return;
    }

    uint8_t chip_id = aw9523_get_chip_id();
    ESP_LOGI(TAG, "Chip ID: 0x%02X (expected 0x23)", chip_id);
    if (chip_id != 0x23) {
        ESP_LOGE(TAG, "FAIL: Unexpected chip ID 0x%02X", chip_id);
        return;
    }
    ESP_LOGI(TAG, "PASS: Phase 1 — chip ID OK");
    vTaskDelay(pdMS_TO_TICKS(100));
```

- [ ] **Step 2: Write Phase 2 — Output pin walk**

```c
    // === Phase 2: Output Pin Walk ===
    ESP_LOGI(TAG, "--- Phase 2: Output Pin Walk (%d pins) ---", g_num_output_pins);

    int pin_pass = 0, pin_fail = 0;
    for (int i = 0; i < g_num_output_pins; i++) {
        uint8_t pin = g_output_pins[i];
        uint8_t port = AW9523_GET_PORT(pin);
        uint8_t bit = AW9523_GET_BIT(pin);

        // Set pin HIGH
        aw9523_set_pin(pin, 1);
        vTaskDelay(pdMS_TO_TICKS(1));
        uint8_t val_high = aw9523_get_pin(pin);

        // Set pin LOW
        aw9523_set_pin(pin, 0);
        vTaskDelay(pdMS_TO_TICKS(1));
        uint8_t val_low = aw9523_get_pin(pin);

        if (val_high == 1 && val_low == 0) {
            ESP_LOGI(TAG, "  P%d.%d: PASS (H=%d L=%d)", port, bit, val_high, val_low);
            pin_pass++;
        } else {
            ESP_LOGE(TAG, "  P%d.%d: FAIL (H=%d L=%d)", port, bit, val_high, val_low);
            pin_fail++;
        }
        vTaskDelay(pdMS_TO_TICKS(10));
    }
```

- [ ] **Step 3: Write Phase 3 — Input pin read**

```c
    // === Phase 3: Input Pin Read ===
    ESP_LOGI(TAG, "--- Phase 3: Input Pin Read ---");
    for (int i = 0; i < g_num_input_pins; i++) {
        uint8_t pin = g_input_pins[i];
        uint8_t port = AW9523_GET_PORT(pin);
        uint8_t bit = AW9523_GET_BIT(pin);
        uint8_t val = aw9523_get_pin(pin);
        ESP_LOGI(TAG, "  P%d.%d: %d", port, bit, val);
    }
```

- [ ] **Step 4: Write Phase 4 — Port-level operations**

```c
    // === Phase 4: Port-Level Operations ===
    ESP_LOGI(TAG, "--- Phase 4: Port-Level Ops ---");

    // Set P0 to all outputs, write 0xAA, read back
    aw9523_set_port_dir(0, 0x00);
    aw9523_write_port(0, 0xAA);
    vTaskDelay(pdMS_TO_TICKS(1));
    uint8_t p0_read = aw9523_read_port(0);
    ESP_LOGI(TAG, "  P0 write 0xAA -> read 0x%02X %s",
             p0_read, (p0_read == 0xAA) ? "PASS" : "FAIL");

    // Set P1 to all outputs, write 0x55, read back
    aw9523_set_port_dir(1, 0x00);
    aw9523_write_port(1, 0x55);
    vTaskDelay(pdMS_TO_TICKS(1));
    uint8_t p1_read = aw9523_read_port(1);
    ESP_LOGI(TAG, "  P1 write 0x55 -> read 0x%02X %s",
             p1_read, (p1_read == 0x55) ? "PASS" : "FAIL");

    int port_pass = ((p0_read == 0xAA) && (p1_read == 0x55)) ? 1 : 0;
```

- [ ] **Step 5: Write Phase 5 — Restore safe state**

```c
    // === Phase 5: Restore Safe State ===
    ESP_LOGI(TAG, "--- Phase 5: Restore Safe State ---");
    // Match aw9523_init defaults:
    // P0: bit5=INT(input), others output
    aw9523_set_port_dir(0, (1 << 5));
    aw9523_write_port(0, 0x00);
    // P1: bit6=BUSY(input), bit4=CS(high), others output
    aw9523_set_port_dir(1, (1 << 6));
    aw9523_write_port(1, (1 << 4));
    ESP_LOGI(TAG, "  Safe state restored");
```

- [ ] **Step 6: Write Phase 6 — Summary**

```c
    // === Phase 6: Summary ===
    ESP_LOGI(TAG, "--- Phase 6: Summary ---");
    ESP_LOGI(TAG, "  Output pins: %d/%d passed", pin_pass, g_num_output_pins);
    bool all_pass = (pin_fail == 0) && port_pass && (chip_id == 0x23);

    if (all_pass) {
        ESP_LOGI(TAG, "=== TEST COMPLETE: ALL PASSED ===");
    } else {
        ESP_LOGE(TAG, "=== TEST COMPLETE: FAILURES DETECTED ===");
    }

    while (1) {
        vTaskDelay(pdMS_TO_TICKS(1000));
    }
}
```

- [ ] **Step 7: Build and verify compilation**

Run: `idf.py build` from `debug/t_aw9523/`
Expected: Compilation succeeds with no errors or warnings

- [ ] **Step 8: Commit**

```bash
git add debug/t_aw9523/main/t_aw9523.c
git commit -m "feat(debug): add t_aw9523 test firmware with full pin validation"
```
